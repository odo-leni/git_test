# git_test

## Git commands

- ```shell 
  git clone git@github.com:odo-leni/git_test.git
  ```
- ```shell 
  git remote -v
  ```
- ```shell 
  git status
  ```
- ```shell 
  git add .
  ```
- ```shell 
  git log
  ```
- ```shell 
  git commit -m "A message describing what you have done to make this snapshot different"
  ```
- ```shell 
  git push origin main
  ```